on:
  push:
    branches: ["main"]

jobs:
  run-npm-build:
    runs-on: ubuntu-latest
    steps:
      - name: npm install and build webpack
        run: |
          npm install
          npm run build

  run-npm-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: npm deploy
        run: npm run deploy
