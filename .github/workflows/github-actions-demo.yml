name: Deploye
on: [push]
jobs:
  run-npm-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: npm install and build webpack
      - run: npm install
      - run: npm run build
      - run: npm run deploy
